import {StyleSheet} from 'react-native';
import {Colors} from 'src/theme';

export default StyleSheet.create({
  heading: {
    fontSize: 18,
    lineHeight: 18,
  },
  headingText: {
    fontSize: 18,
    lineHeight: 24,
  },
  subHeadingText: {
    fontSize: 14,
    lineHeight: 16,
    marginTop: 4,
  },
  checkIcon: {
    width: 24,
    height: 24,
    resizeMode: 'contain',
  },
});
